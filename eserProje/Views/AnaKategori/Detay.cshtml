@model eserProje.Models.MainCategory

@{
    ViewData["Title"] = "Detay";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Detay</h1>

<div class="col-xl-4">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Ana Kategori Detayı</h4>
        </div>
        <div class="card-body">
            <div class="basic-list-group">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item"><strong>ID</strong> : @Model.MainCategoryID</li>
                    <li class="list-group-item"><strong>Ana Kategori Adı</strong> : @Model.MainCategoryName</li>
                </ul>
            </div>
        </div>
    </div>
</div>
<div>
    <a class="btn" asp-action="Duzenle" asp-route-id="@Model?.MainCategoryID">Düzenle</a> |
     <a class="btn" asp-action="Listele">Geri Dön</a>
</div>
