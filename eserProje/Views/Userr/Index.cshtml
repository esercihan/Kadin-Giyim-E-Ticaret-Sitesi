@model IEnumerable<eserProje.Models.Userr>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rolee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rolee.RoleeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserrID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserrID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserrID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
